# Optional: self-hosted deployment
# Set to your domain so sitemap/robots/OG tags point at the right URL.
NEXT_PUBLIC_APP_URL=https://your-domain.com
# Trust x-forwarded-for/x-real-ip for rate limiting only behind a proxy
# you control that overwrites those headers.
CARTOGRAF_TRUST_PROXY_HEADERS=false
